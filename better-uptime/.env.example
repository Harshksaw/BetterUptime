# Database Configuration
DATABASE_URL="postgresql://postgres:password@localhost:5432/betteruptime?schema=public"

# Alternative for Docker network (if running app in Docker)
# DATABASE_URL="postgresql://postgres:password@postgres:5432/betteruptime?schema=public"

# PostgreSQL Configuration
POSTGRES_DB=betteruptime
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# PgAdmin Configuration
PGADMIN_DEFAULT_EMAIL=admin@betteruptime.com
PGADMIN_DEFAULT_PASSWORD=admin
